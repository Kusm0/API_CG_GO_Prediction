{
  "name": "api_cs_go",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "npm run build && pipenv run python -m flask --app app run --debug"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.9",
    "jquery": "^3.7.1"
  },
  "devDependencies": {
    "vite": "^6.0.6"
  }
}
